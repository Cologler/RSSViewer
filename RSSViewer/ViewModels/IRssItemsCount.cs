namespace RSSViewer.ViewModels
{
    /// <summary>
    /// a counter interface use to count the total selected count, etc.
    /// </summary>
    interface IRssItemsCount
    {
        int Count { get; }
    }
}
